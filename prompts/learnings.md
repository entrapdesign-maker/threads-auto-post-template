<!-- このファイルは scripts/measure.py (計測エージェント) が自動生成・更新します。
     手動で編集しても構いませんが、次回の計測実行で上書きされます。
     research / writer エージェントが生成時にこの内容を読み込み、投稿へ反映します。 -->

# 計測フィードバック (初期状態)

まだ計測データがありません。
投稿が貯まり、`scripts/measure.py` が1回でも実行されると、
ここに「数値が伸びた投稿の傾向」が自動でまとまります。
