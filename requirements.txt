# 依存パッケージ
# anthropic: web search サーバーツール (web_search_20260209) を使うため、
#   2026年時点の比較的新しいSDKが必要。メジャー(1.x)の破壊的変更は避ける。
# requests: Threads API 呼び出し用。安定しているのでマイナー以上で許容。
anthropic>=0.69,<1.0
requests>=2.31,<3.0
